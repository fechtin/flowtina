"""Long-term memory subsystem.

Channel-agnostic per-user recall: a verbatim transcript tier plus a distilled,
scored, deduped memory tier with in-process semantic search. Wired into the
comment engagement flow and reused by future channels (e.g. Messenger).
"""
